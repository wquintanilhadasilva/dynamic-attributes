import { InputAttrsDirective } from './input-attrs.directive';

describe('InputAttrsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAttrsDirective();
    expect(directive).toBeTruthy();
  });
});
